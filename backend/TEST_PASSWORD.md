# 🔐 اختبار كلمة السر

## المستخدم صحيح ✅

المستخدم `Hossam` موجود في MongoDB Atlas وملف `.env` - هذا صحيح!

## المشكلة المحتملة: كلمة السر

الخطأ "bad auth" يعني أن كلمة السر غير صحيحة.

---

## الحل:

### 1️⃣ تحقق من كلمة السر في MongoDB Atlas

1. اضغط **"EDIT"** بجانب المستخدم `Hossam`
2. اضغط **"Edit Password"**
3. تأكد من كلمة السر الحالية أو ضبط كلمة سر جديدة
4. ⚠️ **احفظ كلمة السر الجديدة!**

---

### 2️⃣ تحديث ملف .env

بعد التأكد من كلمة السر:

1. افتح `backend/.env`
2. تأكد من السطر:
   ```
   MONGO_URL=mongodb+srv://Hossam:PASSWORD@cluster0.irhqpwj.mongodb.net/?retryWrites=true&w=majority
   ```
3. استبدل `PASSWORD` بكلمة السر الصحيحة

---

### 3️⃣ ترميز الأحرف الخاصة

لو كلمة السر تحتوي على أحرف خاصة، يجب ترميزها:

| الحرف | الترميز |
|-------|---------|
| `@` | `%40` |
| `#` | `%23` |
| `$` | `%24` |
| `%` | `%25` |
| `&` | `%26` |
| `+` | `%2B` |
| `=` | `%3D` |

**مثال:**
- كلمة السر: `MyPass@123`
- في الرابط: `MyPass%40123`

---

### 4️⃣ مثال على ملف .env صحيح

لو كلمة السر `Anjal@123456`:
```
MONGO_URL=mongodb+srv://Hossam:Anjal%40123456@cluster0.irhqpwj.mongodb.net/?retryWrites=true&w=majority
```

لو كلمة السر `MyPassword123` (بدون أحرف خاصة):
```
MONGO_URL=mongodb+srv://Hossam:MyPassword123@cluster0.irhqpwj.mongodb.net/?retryWrites=true&w=majority
```

---

### 5️⃣ اختبار الاتصال

بعد التحديث:
```bash
cd backend
py test_connection.py
```

لو نجح، شغّل السيرفر:
```bash
py -m uvicorn server:app --reload
```

---

## نصيحة:

لو نسيت كلمة السر:
1. اضغط **"EDIT"** بجانب `Hossam`
2. اضغط **"Edit Password"**
3. ضبط كلمة سر جديدة بسيطة (بدون أحرف خاصة)
4. مثال: `MyPassword123`
5. احفظها في ملف `.env`
