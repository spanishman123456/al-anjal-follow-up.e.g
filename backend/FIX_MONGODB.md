# 🔧 إصلاح مشكلة الاتصال بـ MongoDB

## المشكلة
خطأ في الاتصال بقاعدة البيانات MongoDB عند تشغيل السيرفر.

## الحلول الممكنة

### 1️⃣ تحقق من رابط MongoDB

افتح ملف `backend/.env` وتأكد من:
- رابط MongoDB صحيح
- كلمة السر صحيحة
- لا توجد مسافات إضافية

### 2️⃣ تحقق من MongoDB Atlas

1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. سجّل دخول
3. **Network Access** → تأكد إن IP Address مسموح
   - اضغط **Add IP Address** → **Allow Access from Anywhere** (0.0.0.0/0)
4. **Database Access** → تأكد إن المستخدم موجود وكلمة السر صحيحة

### 3️⃣ اختبار الاتصال

شغّل ملف الاختبار:
```bash
cd backend
py test_connection.py
```

هذا سيعطيك معلومات مفصلة عن المشكلة.

### 4️⃣ مشكلة كلمة السر الخاصة

لو كلمة السر تحتوي على أحرف خاصة مثل: `@`, `#`, `$`, `%`, `&`, `+`, `=`

يجب ترميزها في رابط MongoDB:
- استبدل `@` بـ `%40`
- استبدل `#` بـ `%23`
- استبدل `$` بـ `%24`
- استبدل `%` بـ `%25`
- استبدل `&` بـ `%26`
- استبدل `+` بـ `%2B`
- استبدل `=` بـ `%3D`

أو استخدم أداة URL encoding:
https://www.urlencoder.org/

### 5️⃣ مثال على رابط صحيح

```
mongodb+srv://username:password123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

لو كلمة السر `pass@word#123` تصبح:
```
mongodb+srv://username:pass%40word%23123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

### 6️⃣ حل بديل: استخدم MongoDB محلي

لو MongoDB Atlas مش شغال، استخدم MongoDB محلي:

1. ثبت MongoDB من: https://www.mongodb.com/try/download/community
2. شغّل MongoDB
3. في ملف `.env`:
   ```
   MONGO_URL=mongodb://localhost:27017
   ```

---

## بعد الإصلاح

شغّل السيرفر مرة أخرى:
```bash
cd backend
py -m uvicorn server:app --reload
```

أو استخدم ملف `.bat`:
```
start_backend.bat
```
