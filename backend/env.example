# ===========================================
# نسخ هذا الملف إلى .env وعدّل القيم
# في PowerShell من مجلد backend: copy env.example .env
# ===========================================

# إلزامي: رابط MongoDB (Atlas أو محلي)
MONGO_URL=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
# أو محلي: MONGO_URL=mongodb://localhost:27017

# إلزامي: اسم قاعدة البيانات
DB_NAME=school_db

# إلزامي: مفتاح سري لتوقيع JWT (استخدم نصاً عشوائياً طويلاً)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# اختياري: لاستعادة كلمة المرور لجميع المستخدمين (استخدم مرة واحدة ثم احذف هذا السطر)
# RESET_PASSWORD_SECRET=your-one-time-secret

# اختياري: CORS (للمطور اترك * أو أضف روابط الفرونت عند النشر)
# مثال: CORS_ORIGINS=http://localhost:3000,https://your-app.vercel.app
CORS_ORIGINS=*

# --- البريد (SendGrid) - اختياري ---
# SENDER_EMAIL=
# SENDER_NAME=
# SENDGRID_API_KEY=

# --- SMS (Twilio) - اختياري ---
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=
# ADMIN_SMS_NUMBER=
