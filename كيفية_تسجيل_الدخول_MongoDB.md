# 🔐 كيفية تسجيل الدخول إلى MongoDB Atlas

## الطريقة السهلة - تسجيل الدخول

### 1️⃣ اذهب إلى موقع MongoDB Atlas

افتح المتصفح واذهب إلى:
```
https://www.mongodb.com/cloud/atlas
```

### 2️⃣ تسجيل الدخول

#### إذا كان عندك حساب بالفعل:
- اضغط **"Sign In"** (تسجيل الدخول)
- أدخل **البريد الإلكتروني** و **كلمة السر**
- اضغط **Sign In**

#### إذا ما عندك حساب:
- اضغط **"Try Free"** أو **"Sign Up"** (إنشاء حساب)
- اختر **"Sign up with Email"** (التسجيل بالبريد)
- أدخل:
  - **First Name** (الاسم الأول)
  - **Last Name** (اسم العائلة)
  - **Email** (البريد الإلكتروني) ⚠️ **استخدم بريد حقيقي!**
  - **Password** (كلمة السر) - يجب أن تكون قوية (8 أحرف على الأقل)
- اضغط **"Create your Atlas account"**
- ستتلقى رسالة تأكيد على بريدك الإلكتروني
- افتح البريد واضغط على رابط التأكيد

---

## بعد تسجيل الدخول

### 3️⃣ إنشاء قاعدة البيانات المجانية

1. بعد تسجيل الدخول، اضغط **"Build a Database"**
2. اختر **"M0 FREE"** (المجاني)
3. اختر **Provider**: AWS (أو أي واحد)
4. اختر **Region**: الأقرب لك (مثلاً: Frankfurt, Germany)
5. اضغط **"Create"**

### 4️⃣ إعداد المستخدم

1. في صفحة **"Create Database User"**:
   - **Username**: اكتب اسم مستخدم (مثلاً: `admin` أو `dbuser`)
   - **Password**: اكتب كلمة سر قوية ⚠️ **احفظها!**
   - اضغط **"Create Database User"**

### 5️⃣ فتح الشبكة

1. في صفحة **"Network Access"**:
   - اضغط **"Add IP Address"**
   - اختر **"Allow Access from Anywhere"** (0.0.0.0/0)
   - اضغط **"Confirm"**

### 6️⃣ الحصول على رابط الاتصال

1. اضغط **"Finish and Close"**
2. من القائمة الجانبية: اضغط **"Database"**
3. اضغط **"Connect"**
4. اختر **"Drivers"**
5. اختر **"Python"** ونسخة **3.6 or later**
6. انسخ الرابط (يبدأ بـ `mongodb+srv://...`)
7. ⚠️ **مهم جداً**: استبدل `<password>` في الرابط بكلمة السر اللي سجلتها

**مثال:**
```
mongodb+srv://admin:MyPassword123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

---

## تحديث ملف .env

1. افتح ملف: `backend/.env`
2. غيّر السطر:
   ```
   MONGO_URL=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
3. ضع:
   - **USERNAME**: اسم المستخدم اللي سجلته
   - **PASSWORD**: كلمة السر (بدون `<password>`)

---

## لو نسيت كلمة السر

1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. اضغط **"Sign In"**
3. اضغط **"Forgot Password?"**
4. أدخل بريدك الإلكتروني
5. افتح البريد واضغط على رابط إعادة تعيين كلمة السر

---

## لو نسيت اسم المستخدم

1. اذهب إلى: https://www.mongodb.com/cloud/atlas
2. اضغط **"Sign In"**
3. اضغط **"Forgot Username?"**
4. أدخل بريدك الإلكتروني
5. ستتلقى رسالة بالاسم المستخدم

---

## ملاحظات مهمة

⚠️ **احفظ هذه المعلومات:**
- البريد الإلكتروني المستخدم في التسجيل
- كلمة السر
- اسم المستخدم في قاعدة البيانات
- رابط الاتصال (Connection String)

⚠️ **لو كلمة السر تحتوي على أحرف خاصة:**
- `@` → `%40`
- `#` → `%23`
- `$` → `%24`
- `%` → `%25`
- `&` → `%26`
- `+` → `%2B`
- `=` → `%3D`

---

## بعد التحديث

شغّل السيرفر مرة أخرى:
```bash
cd backend
py -m uvicorn server:app --reload
```

أو استخدم:
```
start_backend.bat
```

---

## مساعدة إضافية

لو واجهت أي مشكلة:
1. تأكد من تأكيد البريد الإلكتروني
2. تأكد من فتح Network Access (0.0.0.0/0)
3. تأكد من رابط MongoDB صحيح في ملف `.env`
4. شغّل `test_connection.py` للتحقق من الاتصال
