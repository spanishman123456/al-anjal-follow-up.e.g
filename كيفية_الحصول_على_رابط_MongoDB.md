# 🔗 كيفية الحصول على رابط MongoDB من Atlas

## من الصورة التي أرسلتها:

### الطريقة 1: من زر "Connect" (الأسهل)

1. **اضغط على زر "Connect"** بجانب "Cluster0"
2. ستفتح نافذة جديدة
3. اختر **"Drivers"** (أو "Connect your application")
4. اختر **"Python"** من القائمة
5. اختر النسخة: **"3.6 or later"**
6. انسخ الرابط (يبدأ بـ `mongodb+srv://...`)
7. ⚠️ **مهم**: استبدل `<password>` في الرابط بكلمة السر اللي سجلتها

---

### الطريقة 2: من "Get connection string"

1. في قسم **"Application Development"** (أسفل يسار الشاشة)
2. اضغط على زر **"Get connection string"** (الأخضر)
3. اختر **"Python"** من القائمة المنسدلة "Select"
4. انسخ الرابط
5. استبدل `<password>` بكلمة السر

---

## مثال على الرابط:

```
mongodb+srv://username:<password>@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

**بعد استبدال كلمة السر:**
```
mongodb+srv://username:MyPassword123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
```

---

## تحديث ملف .env

1. افتح ملف: `backend/.env`
2. غيّر السطر:
   ```
   MONGO_URL=mongodb+srv://username:MyPassword123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
3. ضع:
   - **username**: اسم المستخدم اللي سجلته في Database Access
   - **MyPassword123**: كلمة السر (بدون `<password>`)

---

## ⚠️ ملاحظات مهمة:

### 1. تأكد من Database Access:
- اذهب إلى **"Security"** → **"Database Access"** من القائمة الجانبية
- تأكد إن عندك مستخدم موجود
- لو ما عندك، اضغط **"Add New Database User"**

### 2. تأكد من Network Access:
- اذهب إلى **"Security"** → **"Network Access"**
- تأكد إن IP Address مسموح
- لو ما مسموح، اضغط **"Add IP Address"** → **"Allow Access from Anywhere"**

### 3. لو كلمة السر تحتوي على أحرف خاصة:
يجب ترميزها:
- `@` → `%40`
- `#` → `%23`
- `$` → `%24`
- `%` → `%25`
- `&` → `%26`
- `+` → `%2B`
- `=` → `%3D`

---

## بعد التحديث:

1. احفظ ملف `.env`
2. شغّل السيرفر:
   ```bash
   cd backend
   py -m uvicorn server:app --reload
   ```
3. أو استخدم:
   ```
   start_backend.bat
   ```

---

## اختبار الاتصال:

شغّل ملف الاختبار:
```bash
cd backend
py test_connection.py
```

هذا سيعطيك معلومات عن حالة الاتصال.
