# 🚀 كيفية تشغيل نظام دفتر متابعة الطلاب

## 📋 المتطلبات الأساسية (يجب تثبيتها أولاً)

### 1️⃣ Python (للباك اند)
- حمل من: https://www.python.org/downloads/
- أثناء التثبيت: ✅ **اضغط على "Add Python to PATH"**
- تحقق من التثبيت: افتح Terminal واكتب `python --version` أو `py --version`

### 2️⃣ Node.js (للفرونت اند)
- حمل من: https://nodejs.org/ (اختر LTS)
- أثناء التثبيت: ✅ **اضغط على "Add to PATH"**
- تحقق من التثبيت: افتح Terminal واكتب `node --version` و `npm --version`

---

## 🔧 الخطوة 1: إعداد قاعدة البيانات MongoDB

### أ) إنشاء حساب مجاني على MongoDB Atlas:

1. اذهب إلى: **https://www.mongodb.com/cloud/atlas**
2. سجّل حساب جديد (مجاني تماماً)
3. اضغط **"Build a Database"**
4. اختر **"M0 FREE"** (المجاني)
5. اضغط **"Create"**

### ب) إعداد المستخدم:

1. من القائمة الجانبية: **"Database Access"**
2. اضغط **"Add New Database User"**
3. اختر **"Password"** كطريقة المصادقة
4. اكتب:
   - **Username**: (مثلاً: `admin`)
   - **Password**: (مثلاً: `MyPassword123`) ⚠️ **احفظها!**
5. اضغط **"Add User"**

### ج) فتح الشبكة:

1. من القائمة الجانبية: **"Network Access"**
2. اضغط **"Add IP Address"**
3. اختر **"Allow Access from Anywhere"** (أو `0.0.0.0/0`)
4. اضغط **"Confirm"**

### د) الحصول على رابط الاتصال:

1. من القائمة الجانبية: **"Database"**
2. اضغط **"Connect"**
3. اختر **"Drivers"**
4. اختر **"Python"** ونسخة **3.6 or later**
5. انسخ الرابط (يبدأ بـ `mongodb+srv://...`)
6. **⚠️ مهم جداً**: استبدل `<password>` في الرابط بكلمة السر اللي سجلتها
   - مثال: `mongodb+srv://admin:MyPassword123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority`

---

## ⚙️ الخطوة 2: إعداد ملفات الإعدادات

### أ) إعداد الباك اند:

1. افتح مجلد **`backend`**
2. انسخ ملف **`env.example`** وسميه **`.env`**
   - في Windows: كليك يمين على `env.example` → Copy → Paste → سميه `.env`
   - أو من Terminal: `copy env.example .env`
3. افتح ملف **`.env`** بمفكرة النصوص
4. غيّر السطر الأول فقط:
   ```
   MONGO_URL=mongodb+srv://admin:MyPassword123@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
   (ضع الرابط اللي نسخته من MongoDB Atlas)

### ب) إعداد الفرونت اند:

1. افتح مجلد **`frontend`**
2. انسخ ملف **`env.example`** وسميه **`.env`**
   - في Windows: كليك يمين → Copy → Paste → سميه `.env`
   - أو من Terminal: `copy env.example .env`
3. افتح ملف **`.env`** بمفكرة النصوص
4. تأكد إن السطر كده:
   ```
   REACT_APP_BACKEND_URL=http://localhost:8000
   ```
   (لا تغيره - ده للعمل المحلي)

---

## 🎯 الخطوة 3: تشغيل المشروع

### الطريقة السهلة (استخدام ملفات .bat):

#### أ) تشغيل الباك اند:

1. افتح مجلد **`backend`**
2. دبل كليك على ملف **`تشغيل الباك اند.bat`**
3. انتظر حتى يظهر: `INFO:     Uvicorn running on http://0.0.0.0:8000`
4. **⚠️ اترك هذه النافذة مفتوحة!**

#### ب) تشغيل الفرونت اند:

1. افتح مجلد **`frontend`** (في نافذة جديدة)
2. دبل كليك على ملف **`تشغيل الفرونت.bat`**
3. انتظر دقيقة أو دقيقتين
4. سيفتح المتصفح تلقائياً على **http://localhost:3000**

---

### الطريقة اليدوية (من Terminal):

#### أ) تشغيل الباك اند:

1. افتح **Terminal** في Cursor (Terminal → New Terminal)
2. اكتب:
   ```bash
   cd backend
   pip install -r requirements.txt
   uvicorn server:app --reload --host 0.0.0.0 --port 8000
   ```
   أو لو `pip` مش شغال:
   ```bash
   cd backend
   py -m pip install -r requirements.txt
   py -m uvicorn server:app --reload --host 0.0.0.0 --port 8000
   ```
3. انتظر حتى يظهر: `INFO:     Uvicorn running on http://0.0.0.0:8000`
4. **⚠️ اترك هذه النافذة مفتوحة!**

#### ب) تشغيل الفرونت اند:

1. افتح **Terminal جديد** في Cursor (Terminal → New Terminal)
2. اكتب:
   ```bash
   cd frontend
   npm install
   npm start
   ```
3. انتظر دقيقة أو دقيقتين
4. سيفتح المتصفح تلقائياً على **http://localhost:3000**

---

## 🔐 تسجيل الدخول

بعد فتح المتصفح على **http://localhost:3000**:

- **Username**: `admin`
- **Password**: `Admin@123`

---

## ❓ حل المشاكل الشائعة

### مشكلة: "pip غير معروف" أو "npm غير معروف"

**الحل:**
- تأكد إن Python و Node.js مثبتين
- أعد تشغيل Cursor بعد التثبيت
- استخدم `py -m pip` بدل `pip`
- استخدم ملفات `.bat` اللي جاهزة

### مشكلة: "ModuleNotFoundError" في الباك اند

**الحل:**
```bash
cd backend
pip install -r requirements.txt
```
أو:
```bash
cd backend
py -m pip install -r requirements.txt
```

### مشكلة: "Cannot find module" في الفرونت اند

**الحل:**
```bash
cd frontend
npm install
```

### مشكلة: الباك اند مش بيشتغل

**الحل:**
- تأكد إن ملف `.env` موجود في مجلد `backend`
- تأكد إن رابط MongoDB صحيح
- تأكد إن MongoDB Atlas مفتوح للشبكة (Network Access)

### مشكلة: الفرونت اند مش بيشتغل

**الحل:**
- تأكد إن الباك اند شغال أولاً (يجب يكون على http://localhost:8000)
- تأكد إن ملف `.env` موجود في مجلد `frontend`
- جرب حذف مجلد `node_modules` وإعادة التثبيت:
  ```bash
  cd frontend
  rmdir /s node_modules
  npm install
  ```

---

## 📞 لو محتاج مساعدة

لو واجهت أي مشكلة:
1. اكتب لي الخطأ اللي ظهر
2. أو التقط صورة من الشاشة
3. أو انسخ الرسالة من Terminal

---

## ✅ ملخص سريع

1. ✅ ثبت Python و Node.js
2. ✅ اعمل حساب MongoDB Atlas
3. ✅ خذ رابط الاتصال من Atlas
4. ✅ عدّل ملف `backend/.env` برابط MongoDB
5. ✅ شغّل الباك اند (من ملف `.bat` أو Terminal)
6. ✅ شغّل الفرونت اند (من ملف `.bat` أو Terminal)
7. ✅ افتح http://localhost:3000
8. ✅ سجّل دخول بـ `admin` / `Admin@123`

**🎉 تمام! المشروع هيشتغل!**
